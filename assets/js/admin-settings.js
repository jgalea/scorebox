/**
 * Admin settings page JS — color picker initialization.
 *
 * @package ScoreBox
 */

( function( $ ) {
	'use strict';

	$( function() {
		$( '.scorebox-color-picker' ).wpColorPicker();
	} );
} )( jQuery );
